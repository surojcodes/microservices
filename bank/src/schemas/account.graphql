type Account {
  accountNumber: String
  accountType: AccountType
  balance: Float
  customer: Customer
}

input CreateAccountInput {
  accountType: AccountType!
  balance: Float
  customerId: String!
}

enum AccountType {
  SAVINGS
  CHECKING
}
