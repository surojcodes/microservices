type Account {
  accountNumber: String
  accountType: AccountType
  balance: Float
  profile: Profile
}

input CreateAccountInput {
  accountType: AccountType!
  balance: Float
  userId: String!
}

enum AccountType {
  SAVINGS
  CHECKING
}
