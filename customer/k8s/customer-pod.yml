apiVersion: v1
kind: Pod
metadata:
  name: customer-pod
  labels:
    app: customer
spec:
  containers:
    - name: customer
      image: customer:1.0.0
      imagePullPolicy: Never
      env:
        - name: PORT
          value: "3000"
      ports:
        - containerPort: 3000
      resources:
        requests:
          memory: "128Mi"
          cpu: "250m"
        limits:
          memory: "256Mi"
          cpu: "500m"
